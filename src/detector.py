def detect_target(frame):
    """Detect target information from one image frame.

    Return value can later be changed to match the car control protocol, such
    as center_x, center_y, angle, confidence, or line offset.
    """
    if frame is None:
        return None

    # TODO: Add color threshold, line tracking, circle detection, or model inference.
    return None
