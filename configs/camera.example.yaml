# Copy this file to camera.local.yaml for local PC-side tuning.
# Local YAML files are ignored by Git.

pc_camera:
  id: 0
  width: 640
  height: 480
  fps: 30

maixcam_reference:
  width: 512
  height: 320
  fps: 60
  buffer_num: 1
  detect_every_n_frames: 2

target:
  mode: perspective
  perspective_fallback_blob: true
  smoothing_alpha_x100: 35

laser:
  color: green
  use_background_calib: true
  background_calib_frames: 25
  target_background_calib_frames: 15
  require_target: true

uart:
  enabled: false
  output_mode: aim
  baudrate: 115200
