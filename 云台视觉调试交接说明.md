# 云台视觉跟踪调试交接说明

> 工程：`M0_Templant_FreeRTOS`<br>
> 硬件：MSPM0G3507 + 两个 ZDT X42S 步进驱动器 + MaixCAM 视觉模块<br>
> 本文只说明云台（yaw / pitch）视觉跟踪链路；小车双轮、IMU 航向闭环与它相互独立。

## 1. 一句话说明系统如何工作

相机识别到目标后，持续通过串口发送目标中心在 `320 × 240` 图像中的像素坐标。MSPM0 将目标坐标与画面中心 `(160, 120)` 相减，得到：

- `dx = target_x - 160`：水平方向像素误差，控制 **yaw 轴**；
- `dy = target_y - 120`：竖直方向像素误差，控制 **pitch 轴**。

每隔 25 ms，程序用每个轴各自独立的 PD 算法，把像素误差换算成一条“向 CW / CCW 走若干脉冲”的位置命令，并分别通过两个 UART 发给两个 X42S 驱动器。

```text
MaixCAM 图像识别
      │  AIM,...,target_x,target_y,...（UART3）
      ▼
像素误差 dx / dy ──> yaw / pitch 各自 PD ──> 脉冲位置命令
                                               │
                          UART2 ───────────────┤──> yaw X42S
                          UART1 ───────────────└──> pitch X42S
```

## 2. 三个 UART 的职责与接线

三个串口均为 **115200、8N1**，且必须所有设备共地。

| 功能 | MSPM0 外设 | MSPM0 引脚 | 对端 | 方向 / 用途 |
|---|---|---|---|---|
| yaw 步进轴 | UART2 | PA23 = TX，PA24 = RX | yaw X42S，地址 `0x01` | 下发 yaw 位置命令，并接收该驱动器的应答 |
| pitch 步进轴 | UART1 | PB6 = TX，PB7 = RX | pitch X42S，地址 `0x01` | 下发 pitch 位置命令，并接收该驱动器的应答 |
| 视觉与遥测 | UART3 | PB3 = RX，PB2 = TX | MaixCAM / USB-TTL | PB3 接收相机识别结果；PB2 输出 MCU 遥测 |

注意事项：

1. 两个 X42S 都可使用地址 `0x01`，因为它们在**两条物理隔离的 UART 总线**上，不会冲突。
2. UART3 的相机接线应交叉：`相机 TX -> PB3 (MSPM0 RX)`；若需要 MCU 回传给相机或电脑：`PB2 (MSPM0 TX) -> 对端 RX`。
3. 调试电脑若只需要看数据，只接 `PB2 -> USB-TTL RX` 和 GND 即可；不要接到 PA0，PA0 是 I2C 引脚。
4. 外部晶振曾出现启动不稳定问题；本工程使用内部时钟配置，调试时不要随意改回外部晶振。

对应代码：

- UART 引脚和波特率：`ti_msp_dl_config.h`
- 步进双 UART 驱动：`Component/UART/stepper_uart.c`
- 步进命令队列 / 应答轮询：`Component/service/stepper_service.c`
- 相机收包与遥测发送：`Component/vision/vision_uart.c`

## 3. “反馈”到底是什么

这套控制要区分三个层次，避免把它误认为传统的“编码器位置闭环”。

| 层次 | 被控对象 | 反馈来源 | 本工程是否直接读取 |
|---|---|---|---|
| 视觉外环 | 目标在画面中的位置 | MaixCAM 给出的 `target_x / target_y` | 是，`dx / dy` 是 PD 的输入 |
| 云台执行层 | yaw、pitch 两个 X42S 的转动 | X42S 驱动器内部的闭环 / 位置执行 | MCU 不读取电机编码器值，只接收协议应答和到位应答 |
| 小车航向环 | 车体 yaw | ICM20602 IMU | 独立功能，不参与云台视觉跟踪 |

因此，云台视觉跟踪是“**视觉像素闭环 + 驱动器内部位置执行**”。如果云台实际移动了但目标没有回到画面中心，下一帧的 `dx/dy` 会继续产生修正命令。

它不是绝对机械角度闭环：当前 MCU 没有把云台实际角度读回来，也没有建立“像素 → 角度”的标定模型。若日后需要精确指向某个物理角度，需要增加机械零位、编码器角度读取或标定后的像素-角度模型。

## 4. 相机协议与解析规则

当前 MaixCAM 示例输出类似：

```text
AIM,0,0,0,199,105,0,0,TEMPORAL,NO_LASER
```

程序使用其中第 4、5 个数值（从 0 开始计数的 `field[3]`、`field[4]`）：

```text
target_x = 199
target_y = 105
dx = 199 - 160 = +39 px
dy = 105 - 120 = -15 px
```

解析器已特别处理一个容易误解的问题：`AIM` 后的第一个 `0/1` 是瞄准/激光状态，不是目标有效标志。对 `AIM` 行，只要末尾状态不是 `NO_TARGET` 或 `LOST`，就会将该帧视为目标有效；所以 `TEMPORAL,NO_LASER` **仍会参与跟踪**。

程序也接受简化遥测格式：

```text
TV,valid,unused,unused,target_x,target_y
```

图像尺寸目前固定为 `320 × 240`。若相机输出分辨率改变，必须同步修改 `Component/vision/vision_uart.c` 中的 `VISION_FRAME_WIDTH`、`VISION_FRAME_HEIGHT`，否则中心点和越界校验都会错误。

## 5. 当前 PD 控制器与参数位置

参数文件：`Component/config/vision_tracking_config.h`

当前配置如下：

| 参数 | yaw | pitch | 含义 |
|---|---:|---:|---|
| `Kp` | `0.28 pulse/pixel` | `0.40 pulse/pixel` | 像素误差越大，单次移动脉冲越多 |
| `Kd` | `0.000 pulse·s/pixel` | `0.005 pulse·s/pixel` | 对误差变化率的阻尼项 |
| 死区 | `±4 px` | `±4 px` | 误差进入死区后不再发运动命令 |
| 最大单次命令 | `400 pulses` | `400 pulses` | 防止异常帧造成大幅跳动 |
| 命令间隔 | `25 ms` | `25 ms` | PD 输出和位置命令的最低间隔 |
| X42S 速度 | `60 rpm` | `60 rpm` | 每条位置命令的执行速度 |
| 加速度 | `20` | `20` | X42S 协议中的加速度参数 |
| 正误差方向 | `CCW` | `CW` | 已按当前机械装配配置 |

实际公式（在死区外）为：

```text
P = error_pixels × Kp
D = (error_pixels - previous_error_pixels) / dt × Kd
output_pulses = clamp(P + D, -400, +400)
```

然后根据轴方向配置把带符号脉冲转换为 CW / CCW 与绝对脉冲数。控制器是 **PD，不含 I 项**，以免目标静止、视觉抖动时积分不断累积。

> 当前 yaw 参数来自 120 脉冲扰动的 3 次粗搜复测：3/3 次稳定，中位回中时间 0.95 s，稳定窗口平均误差约 2 px，未观察到过零振荡。pitch 仍沿用原可用参数，尚未完成同等覆盖的网格搜索；这是一版可用初值，不是最终比赛标定。

## 6. 运行节拍和任务关系

| 任务 / 服务 | 周期 | 做什么 |
|---|---:|---|
| `vision_tracking_task` | 1 ms 轮询 | 消化 UART3 字节、解析新相机帧、维护调试快照、发送遥测 |
| yaw / pitch PD 命令 | 最快每 25 ms 一次 | 对最新有效目标各计算一次并提交位置命令 |
| `stepper_service_task` | 1 ms | 从命令队列取出命令、发到 UART1/2、轮询两个驱动器的应答 |
| UART 接收中断 | 按字节 | UART3 收相机数据；UART1/2 收 X42S 响应字节 |
| UART3 遥测 | 20 ms | 从 PB2 发往电脑 / 相机 |

关键点：视觉任务即使 1 ms 运行，也不会每 1 ms 都让电机走一次；实际动作频率受 `VISION_TRACKING_COMMAND_PERIOD_MS = 25` 限制，约为 40 Hz 上限。

## 7. 如何调参

### 先确认方向，再调增益

烧录后把目标缓慢移到画面右侧、左侧、上侧、下侧，逐轴观察：

- 目标在右边（`dx > 0`）时，yaw 是否朝着让目标回到中心的方向移动；
- 目标在下边（`dy > 0`）时，pitch 是否朝正确方向移动；
- 方向错误时，先只修改对应的 `VISION_TRACKING_*_POSITIVE_IS_CW`，不要通过把 `Kp` 改成负数修正。

### 推荐顺序

1. 暂时将 `Kd = 0`，从很小 `Kp` 开始。
2. 以目标从中心快速移动到画面边缘为测试，逐步提高 `Kp`，直到收敛明显变快但开始出现过冲或来回摆动。
3. 将 `Kp` 回退一点，再逐步增加 `Kd`，只加到能明显压住过冲的位置。
4. 若中心附近仍频繁抖动，优先适当增大死区（例如 `4 -> 5/6 px`），不要盲目增大 D。
5. 若大误差时移动太慢，再考虑提高 `VISION_TRACKING_SPEED_RPM` 或 `MAX_PULSES`；这会增大冲击和过冲风险。

### 现象与常用处理

| 现象 | 常见原因 | 优先处理 |
|---|---|---|
| 一上电就向一侧持续转 | 相机未收到有效数据却被错误解析，或方向配置错误 | 先检查 `last_target_valid`、`dx/dy`，再检查方向 |
| 目标越偏，云台越往反方向走 | 轴方向相反 | 修改该轴 `POSITIVE_IS_CW` |
| 来回摆头 | `Kp` 过大、命令过密、机械间隙或视觉抖动 | 降 `Kp`，再小幅加 `Kd`，必要时加大死区 |
| 回中心很慢但不摆 | `Kp` 太小、rpm 太低、单次脉冲受限 | 先加 `Kp`，后再加速度 / 最大脉冲 |
| yaw 正常、pitch 不动 | UART1 接线、驱动器使能、PB6/PB7 配置或指令响应问题 | 查 UART1 与 pitch 状态 |
| 只收到相机数据但电机不动 | 目标被判无效、在死区内、命令队列满或步进服务未启动 | 看遥测与调试变量 |

## 8. 可直接读取的调试数据

### UART3 遥测（推荐）

MSPM0 从 **PB2 (UART3 TX)** 每 20 ms 输出一行：

```text
VT,time_ms,target_valid,dx,dy,yaw_pulses,pitch_pulses
```

例如：

```text
VT,12340,1,39,-15,-8,-6
```

含义是：当前目标有效，目标在画面中心右 39 px、上 15 px；本周期 yaw 下发 -8 脉冲，pitch 下发 -6 脉冲。将 USB-TTL 的 **RX 接 PB2** 并共地，用 115200 波特率打开串口即可记录数据和画曲线。

### Keil Watch / MCP Debug

可观察全局变量：

```c
g_vision_tracking_debug
```

重点字段：

- `uart_packet_count`：有效帧数，应持续增加；
- `uart_parse_error_count`：协议错误数，正常应不持续增长；
- `uart_overrun_count`：接收队列溢出数，正常应为 0；
- `last_target_valid`、`last_target_x/y`：当前目标状态与坐标；
- `last_error_x/y_pixels`：调 PD 最关键的 `dx/dy`；
- `yaw/pitch_p_term_milli_pulses`、`yaw/pitch_d_term_milli_pulses`：P/D 分量；
- `yaw/pitch_output_pulses`：最终计算出的带符号脉冲；
- `yaw/pitch_command_count`：成功提交到步进服务的命令数。

读这个结构时，可先读 `update_sequence`，再读字段，最后再读一次 `update_sequence`；前后相同且为偶数时，说明这一快照没有在读取中间被更新。

## 9. 代码入口与启停

- 视觉跟踪开关：`VISION_TRACKING_ENABLED`，位于 `Component/config/vision_tracking_config.h`。
- 跟踪任务入口：`Component/app/vision_tracking.c` 的 `vision_tracking_task()`。
- 任务创建位置：`Component/task/app_tasks.c`。
- 启动时，视觉任务会初始化 UART3，并向 yaw / pitch 两轴依次投递“使能”命令。

为避免和功能验证程序抢同一组步进电机，进行视觉调试时应确保 `actuator_validation` 的功能验证开关处于关闭状态。一个轴只应有一个上层逻辑持续下发位置命令。

## 10. 安全与排障检查表

烧录前后按以下顺序检查：

1. 相机、MCU、两个 X42S 的 GND 共地。
2. 相机 TX 确实接 PB3；若看 MCU 遥测，USB-TTL RX 确实接 PB2。
3. yaw 为 PA23/PA24，pitch 为 PB6/PB7；不要把两轴接到同一条 UART。
4. 两驱动器上电且可使能；两个地址同为 `0x01` 是正确的。
5. 按复位后观察 LED 是否继续闪烁。若停止闪烁，优先用 `g_boot_stage` 判断卡在初始化的哪一步。
6. 先不挂机械负载或将云台留出安全行程，确认每轴方向正确后再进行高增益测试。
7. 发现异常连续旋转时，立刻断开步进电机供电或停掉视觉任务，先读取 `dx/dy` 和 `target_valid`，不要继续加大 PID 参数。

## 11. 与小车部分的关系

工程里还有一个名称也叫 `yaw` 的功能：它使用 ICM20602 与左右轮差速控制小车航向。这和云台的 **yaw 步进轴** 不是同一个控制环。

- 小车航向环：IMU 反馈 → 左右 MG680 轮电机速度目标；
- 云台 yaw 轴：相机 `dx` 反馈 → UART2 → X42S 位置脉冲；
- 云台 pitch 轴：相机 `dy` 反馈 → UART1 → X42S 位置脉冲。

调云台视觉时，不需要改小车速度环 PID，也不需要使用 ICM20602 的姿态数据。
